{
  "leave_on_terminate": true,
  "bind_addr": "$(MY_POD_IP)",
  "client_addr": "$(MY_POD_IP)",
  "retry_join": ["$(CONSUL_SERVER)"],
  "service": {
    "name": "db",
    "port": 8082,
    "checks": [{
      "http": "http://localhost:8082/db",
      "interval": "10s",
      "timeout": "1s"
    }],
    "connect": { "proxy": {} }
  }
}
