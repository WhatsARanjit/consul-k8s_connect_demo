{
  "leave_on_terminate": true,
  "bind_addr": "$(MY_POD_IP)",
  "client_addr": "$(MY_POD_IP)",
  "retry_join": ["172.17.0.4"],
  "service": {
    "name": "db",
    "port": 8081,
    "checks": [{
      "http": "http://localhost:8081/db",
      "interval": "10s",
      "timeout": "1s"
    }],
    "connect": { "proxy": {} }
  }
}
